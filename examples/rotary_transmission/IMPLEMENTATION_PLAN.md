# Rotary transmission evaluator implementation plan

1. Lock and hash the evaluator-owned physical contract before recorded candidates.
2. Add missing STARK observation bindings and tests for contact reactions, per-tet deformation, energy, and runtime policy auditing.
3. Implement evaluator-owned D shafts, mount, connector insertion, settle, torque-limited speed control, output brake, and a minimal dynamic contact-only transfer test.
4. Generate and independently validate separate-solid STEP candidates; record every substantive attempt and immutable hashes.
5. Iterate without changing the frozen contract until a candidate passes actual observations; repeat twice from clean deterministic inputs.
6. Render only actual CAD and recorded STARK states, then assemble and inspect 4K/1080p client deliverables with evidence and simulation disclaimer.

No video success sequence may be produced until gates 1–5 pass. Numerical failure is reported as `INCONCLUSIVE_NUMERICS`.
